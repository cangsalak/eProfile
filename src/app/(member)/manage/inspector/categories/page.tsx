import { DataCategorySettingsView } from '@/modules/system-inspector';

export const metadata = {
  title: 'Data Categories Settings',
};

export default function DataCategorySettingsPage() {
  return <DataCategorySettingsView />;
}
